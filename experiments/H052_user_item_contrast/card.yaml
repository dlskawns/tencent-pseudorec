# H052 — Experiment Card (2026-05-08)
hypothesis_id: H052_user_item_contrast
control_exp_id: H019_twin_long_seq_retrieval

mutation:
  class: representation_contrastive
  type: in_batch_infonce_auxiliary
  description: |
    H019 base + User-Item InfoNCE auxiliary on representations.
    forward returns (logits, user_repr=backbone_out, item_repr=item_ns_mean).
    trainer: sim = (u @ it.T) / tau, target = arange(B), contrast_loss = CE(sim, target).
    contrast_lambda=0.1, contrast_temperature=0.1.
    0 trainable params 추가.

compute_tier: T2.4
expected_runtime_h: 3.5
cost_estimate_usd: 6.0

success_criterion:
  primary: "Δ vs H019 (0.839674) Platform AUC ≥ +0.001pt"

build_status:
  upload_package: BUILT
  T0_sanity: ALL PASS
  cloud_ready: true
  infer_flag_parity: VERIFIED
