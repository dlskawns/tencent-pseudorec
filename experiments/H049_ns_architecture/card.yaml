# H049 — Experiment Card (2026-05-08)
hypothesis_id: H049_ns_architecture
control_exp_id: H019_twin_long_seq_retrieval

mutation:
  class: ns_architecture
  type: ns_count_expansion_plus_type_embedding
  description: |
    H019 base + NS architecture refinement:
      (a) item_ns_tokens 2 → 6 (item representation slot expansion)
      (b) NS slot type embedding (Embedding(4, D), 4 types: user_ns/user_dense/
          item_ns/item_dense) added to ns_tokens before backbone
    둘 다 NS structure 변경 = single concept mutation = "NS architecture refinement".
    ~16K trainable params.

compute_tier: T2.4
expected_runtime_h: 3.5
cost_estimate_usd: 6.0

success_criterion:
  primary: "Δ vs H019 (0.839674) Platform AUC ≥ +0.001pt"

build_status:
  upload_package: BUILT
  cloud_ready: true
  infer_flag_parity: VERIFIED
